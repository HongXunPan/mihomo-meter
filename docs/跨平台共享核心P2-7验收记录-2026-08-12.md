# 跨平台共享核心 P2-7 验收记录

## 1. 结论

- 状态：通过
- 确认日期：2026-08-12
- 验收范围：代理类型共享优先、原生懒回退最终运行态门禁
- 证据来源：用户按 P2-7 运行态验收指南完成双端实机验证并明确确认双端通过

本记录只保留平台、构建、时长和粗粒度结论，不包含完整诊断日志、原始代理类型、Controller Secret、订阅地址、节点、链路、连接目标或真实响应。

## 2. 构建与环境

| 平台 | 系统与权限 | 验收构建 | 启动与时长 |
| --- | --- | --- | --- |
| macOS | macOS 26.5.2（25F84，x86_64）真实设备 | `0720cc1051de75d3375fddaa2a92ad56b26c142e` 源码 Debug 构建 | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |
| Windows | Windows 10 22H2 x64 标准用户 | `0720cc1051de75d3375fddaa2a92ad56b26c142e` 对应 CI 预览产物 | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |

双端连续连接均覆盖“空闲 → 明确 Proxy → 明确 DIRECT → 再次空闲”，并核对现有分类、累计账本、连接状态和资源表现。

## 3. 运行态结果

| 门禁 | macOS | Windows |
| --- | --- | --- |
| 共享核心启动探针 `ready` | 通过 | 通过 |
| `shared_primary + succeeded` | 通过 | 通过 |
| 单进程同类事件去重 | 通过 | 通过 |
| Proxy / DIRECT / 空闲切换 | 通过 | 通过 |
| 旧生产影子或 `matched` 事件 | 无 | 无 |
| 禁止回退状态 | 无 | 无 |
| 分类、账本与资源表现 | 通过 | 通过 |

双端未报告 ABI、原生调用、输入、结果、差异或未知失败，也未发现敏感内容、重复诊断失控、连接异常或明显性能退化。自然出现的共享未识别仍允许按契约恰好回退一次原生分类，本次放行不改变未知流量口径。

## 4. 工程门禁

- P2-6 由 PR #55 合并，生产装配已切换为共享识别成功直接返回、未识别或共享错误时懒回退原生分类。
- 验收基线的 [Windows 构建与测试 run 31597518500](https://github.com/HongXunPan/mihomo-meter/actions/runs/31597518500) 已通过，Windows 实机使用该 commit 对应产物。
- 同一主分支基线的[通用 CI run 31597518518](https://github.com/HongXunPan/mihomo-meter/actions/runs/31597518518) 已通过。
- macOS 与 Windows 双端实机结果由用户最终确认。

## 5. 最终放行边界

P2-7 双端运行态门禁通过，P2 代理类型共享分类阶段完成。生产保持共享优先、原生懒回退；`unrecognized` 和所有共享错误仍必须且只能执行一次原生回退。

本次通过不删除原生 `ProxyClassifier`、P2-4 受保护路由、统一向量或确定性差分测试，也不扩大到 View、网络、Controller 解码、凭据、持久化、连接增量、速率或账本。后续如需删除回滚入口或扩大共享边界，必须另立方案和门禁。
