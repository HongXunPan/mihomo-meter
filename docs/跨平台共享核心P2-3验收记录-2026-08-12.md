# 跨平台共享核心 P2-3 验收记录

## 1. 结论

- 状态：通过
- 确认日期：2026-08-12
- 验收范围：代理类型共享分类生产影子
- 证据来源：用户按运行态验收指南完成双端实机验证并明确确认双端通过

本记录只保留平台、构建、时长和粗粒度结论，不包含完整诊断日志、原始代理类型、Controller Secret、订阅地址、节点、链路、连接目标或真实响应。

## 2. 构建与环境

| 平台 | 系统与权限 | 验收构建 | 启动与时长 |
| --- | --- | --- | --- |
| macOS | macOS 26.5.2（25F84，x86_64）真实设备 | `bce75a32492a344865026f0e2b432bbc1d94958d` 源码 Debug 构建 | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |
| Windows | Windows 10 22H2 x64 标准用户 | `bce75a32492a344865026f0e2b432bbc1d94958d` 对应 CI 预览产物 | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |

双端连续连接均覆盖“空闲 → 明确 Proxy → 明确 DIRECT → 再次空闲”，并核对现有流量分类、累计账本、连接状态和资源表现。

## 3. 运行态结果

| 门禁 | macOS | Windows |
| --- | --- | --- |
| 共享核心启动探针 `ready` | 通过 | 通过 |
| `shared_shadow + matched` | 通过 | 通过 |
| 单进程同类事件去重 | 通过 | 通过 |
| Proxy 场景 | 通过 | 通过 |
| DIRECT 场景 | 通过 | 通过 |
| 空闲切换 | 通过 | 通过 |
| 禁止状态 | 无 | 无 |
| 分类、账本与资源表现 | 通过 | 通过 |

双端未报告 `abi_mismatch`、`native_call_failed`、`unsupported_input`、`input_too_long`、`unexpected_result`、`mismatch` 或 `unknown_failure`，也未发现敏感内容、重复诊断失控、连接异常或明显性能退化。自然出现的共享未识别仍允许按既定契约回退原生分类，本次放行不改变未知流量口径。

## 4. 工程门禁

- P2-F0 至 P2-2 分别由 PR #48 至 PR #51 合并，依次修正原生基线、锁定方案、建立代理分类 ABI 并接入生产影子。
- 验收基线的 [Windows 构建与测试 run 31583370661](https://github.com/HongXunPan/mihomo-meter/actions/runs/31583370661) 已通过，Windows 实机使用该 commit 对应产物。
- 同一主分支基线的[通用 CI run 31583370595](https://github.com/HongXunPan/mihomo-meter/actions/runs/31583370595) 已通过。
- macOS 与 Windows 双端实机结果由用户最终确认。

## 5. 放行边界

P2-3 双端运行态门禁通过，可以进入 P2-4 受保护主路径。P2-4 只有在共享分类与逐次计算的原生分类精确一致时才允许返回共享分类；未识别类型、适配器异常、加载异常或结果不一致仍必须返回原生分类。

本次通过不停止逐次原生比较，不删除原生 `ProxyClassifier`、统一向量或差分测试，也不扩大到 View、网络、Controller 解码、凭据、持久化、连接增量、速率或账本。
