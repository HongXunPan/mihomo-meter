# 跨平台共享核心 P2-5 验收记录

## 1. 结论

- 状态：通过
- 确认日期：2026-08-12
- 验收范围：代理类型共享分类受保护主路径
- 证据来源：用户按运行态验收指南完成双端实机验证并明确确认双端通过

本记录只保留平台、构建、时长和粗粒度结论，不包含完整诊断日志、原始代理类型、Controller Secret、订阅地址、节点、链路、连接目标或真实响应。

## 2. 构建与环境

| 平台 | 系统与权限 | 验收构建 | 启动与时长 |
| --- | --- | --- | --- |
| macOS | macOS 26.5.2（25F84，x86_64）真实设备 | `d10627e4b9c9415288dc6b68e41e2675747636a5` 源码 Debug 构建 | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |
| Windows | Windows 10 22H2 x64 标准用户 | `d10627e4b9c9415288dc6b68e41e2675747636a5` 对应 CI 预览产物 | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |

双端连续连接均覆盖“空闲 → 明确 Proxy → 明确 DIRECT → 再次空闲”，并核对现有分类、累计账本、连接状态和资源表现。

## 3. 运行态结果

| 门禁 | macOS | Windows |
| --- | --- | --- |
| 共享核心启动探针 `ready` | 通过 | 通过 |
| `shared_primary + matched` | 通过 | 通过 |
| 单进程同类事件去重 | 通过 | 通过 |
| Proxy / DIRECT / 空闲切换 | 通过 | 通过 |
| 旧生产影子事件 | 无 | 无 |
| 禁止回退状态 | 无 | 无 |
| 分类、账本与资源表现 | 通过 | 通过 |

双端未报告 ABI、原生调用、输入、结果、差异或未知失败，也未发现敏感内容、重复诊断失控、连接异常或明显性能退化。自然出现的共享未识别仍允许按契约回退原生分类，本次放行不改变未知流量口径。

## 4. 工程门禁

- P2-4 由 PR #53 合并，生产注入已从影子切换为共享与原生精确一致才返回共享分类的受保护主路径。
- 验收基线的 [Windows 构建与测试 run 31587014835](https://github.com/HongXunPan/mihomo-meter/actions/runs/31587014835) 已通过，Windows 实机使用该 commit 对应产物。
- 同一主分支基线的[通用 CI run 31587014888](https://github.com/HongXunPan/mihomo-meter/actions/runs/31587014888) 已通过。
- macOS 与 Windows 双端实机结果由用户最终确认。

## 5. 放行边界

P2-5 双端运行态门禁通过，可以进入 P2-6 共享优先、原生懒回退。P2-6 只有共享核心返回可识别的 Proxy、DIRECT 或 REJECT 时才允许直接采用共享分类；`unrecognized` 和所有共享错误必须且只能执行一次原生回退。

本次通过不删除原生 `ProxyClassifier`、P2-4 受保护路由、统一向量或确定性差分测试，也不扩大到 View、网络、Controller 解码、凭据、持久化、连接增量、速率或账本。
