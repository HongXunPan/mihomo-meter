# 跨平台共享核心 P1.3 验收记录

## 1. 结论

- 状态：通过
- 确认日期：2026-08-12
- 验收范围：字节数、完整速率和紧凑速率受保护主路径
- 证据来源：用户按运行态验收指南完成双端实机验证并明确确认 P1.3 双端通过

本记录只保留平台、构建、时长和粗粒度结论，不包含完整诊断日志、Controller Secret、订阅地址、节点、路径、连接目标或真实响应。

## 2. 构建与环境

| 平台 | 系统与权限 | 验收构建 | 启动与时长 |
| --- | --- | --- | --- |
| macOS | macOS 26.5.2（25F84，x86_64）真实设备 | `dd891d35dc9f8cfff1567b9c5e982d020cf9683c` | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |
| Windows | Windows 10 22H2 x64 标准用户 | `7281af91ca6279802410a8d9cee11be3a4322c6f` | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |

双端连续连接均覆盖“空闲 → 产生流量 → 再次空闲”，并保持十进制 1000 进位和既有 macOS 精度口径。Windows 使用设置窗口 Owner 修复合并后的主分支产物完成最终复验。

## 3. 运行态结果

| 门禁 | macOS | Windows |
| --- | --- | --- |
| 启动探针 `ready` | 通过 | 通过 |
| `byte_count shared_primary` | 通过 | 通过 |
| `rate shared_primary` | 通过 | 通过 |
| `compact_rate shared_primary` | 通过 | 通过 |
| 单进程同类正向事件去重 | 通过 | 通过 |
| `native_fallback` | 无 | 无 |
| 影子或失败事件 | 无 | 无 |
| 现行展示保持 | 通过 | 通过 |
| 启动、连接与性能 | 通过 | 通过 |

诊断未出现敏感内容、重复失控或 `WINDOWS_FAILURE`。Windows 设置窗口保持在主窗口之上，重复入口仍复用同一实例。

## 4. 工程门禁

- P1.3-0 至 P1.3-4 分别由 PR #35 至 PR #39 合并，三类格式按阶段进入受保护主路径。
- Windows 设置窗口层级修复由 PR #40 合并，最终 Windows 验收基线为 `7281af91ca6279802410a8d9cee11be3a4322c6f`。
- 最终 Windows 基线对应[构建与测试 run 31554732014](https://github.com/HongXunPan/mihomo-meter/actions/runs/31554732014)，静态、测试、构建、发布与打包门禁通过。
- macOS 验收基线已通过对应主分支通用 CI；双端实机结果由用户最终确认。

## 5. 放行边界

P1.3 双端运行态门禁通过，三类流量格式继续使用受保护主路径。共享文本只有与原生文本精确一致时才返回；任何异常仍按单次调用回退原生文本。

本次通过不删除原生格式化实现，不移除逐次原生一致性护栏，也不扩大到 View、窗口生命周期、网络、Controller 解码、凭据、SQLite、本地化文案或异步调度。是否进入 P1.4 必须基于独立方案、回滚设计和门禁决定。
