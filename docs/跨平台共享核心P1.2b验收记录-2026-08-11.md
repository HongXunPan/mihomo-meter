# 跨平台共享核心 P1.2b 验收记录

## 1. 结论

- 状态：通过
- 确认日期：2026-08-11
- 验收范围：字节数、完整速率和紧凑速率生产影子比较
- 证据来源：用户按运行态验收指南完成双端实机验证并明确确认 P1.2b 通过

本记录只保留平台、构建、时长和粗粒度结论，不包含完整诊断日志、Controller Secret、订阅地址、节点、路径、连接目标或真实响应。

## 2. 构建与环境

| 平台 | 系统与权限 | 验收构建 | 启动与时长 |
| --- | --- | --- | --- |
| macOS | macOS 26.5.2（25F84，x86_64）真实设备 | `8f0083a5b247480f48e4adb3c1b18c2a0368bf2e` | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |
| Windows | Windows 10 22H2 x64 标准用户 | `b269a2ca121cf58937cf60440af95b791284bdef` | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |

执行日期为 2026-08-11。双端连续连接均覆盖“空闲 → 产生流量 → 再次空闲”，并保持现行十进制 1000 进位和 macOS 精度口径。

## 3. 运行态结果

| 门禁 | macOS | Windows |
| --- | --- | --- |
| 启动探针 `ready` | 通过 | 通过 |
| `byte_count matched` | 通过 | 通过 |
| `rate matched` | 通过 | 通过 |
| `compact_rate matched` | 通过 | 通过 |
| 单进程同类正向事件去重 | 通过 | 通过 |
| 异常事件 | 无 | 无 |
| 现行展示保持 | 通过 | 通过 |

未出现 `abi_mismatch`、`native_call_failed`、`unsupported_traffic_unit`、`unexpected_result`、`mismatch`、`unknown_failure` 或对应 `WINDOWS_FAILURE`。

## 4. 工程门禁

- P1.2b 生产影子比较基线已通过 macOS 与 Windows CI。
- macOS 共享核心静态链接修复已由 PR #33 合并，合并提交为 `e0d93a428c734de364376f9f965275417eff12d5`。
- Windows 验收构建对应完整静态、测试、构建、发布与打包门禁已通过。

## 5. 放行边界

P1.2b 双端运行态门禁通过，只放行 P1.3 主路径评估。生产输出继续由原生算法决定，不自动切换共享核心，不删除原生实现，也不扩大到网络、持久化、凭据、窗口生命周期或 View。

P1.3 必须先确定主路径切换边界、原生回退策略、异常降级、对照测试和可回滚发布步骤，再单独申请实现与验收。
