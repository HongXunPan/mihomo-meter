# 跨平台共享核心 P1.4 验收记录

## 1. 结论

- 状态：通过
- 确认日期：2026-08-12
- 验收范围：字节数、完整速率和紧凑速率共享优先、原生懒回退路径
- 证据来源：用户完成双端实机验证并明确确认 P1.4 双端通过

本记录只保留平台、构建、时长和粗粒度结论，不包含完整诊断日志、Controller Secret、订阅地址、节点、路径、连接目标或真实响应。

## 2. 构建与环境

| 平台 | 系统与权限 | 验收构建 | 启动与时长 |
| --- | --- | --- | --- |
| macOS | macOS 26.5.2（25F84，x86_64）真实设备 | `307912d700fc650f469316ebc4fe1bf73aa9394d` 源码 Debug 构建 | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |
| Windows | Windows 10 22H2 x64 标准用户 | `mihomo-meter-windows-w3-preview-0.0.0-307912d700fc650f469316ebc4fe1bf73aa9394d` | 2 次独立启动，包含冷启动；连续连接至少 30 分钟 |

双端连续连接均覆盖“空闲 → 产生流量 → 再次空闲”，并保持十进制 1000 进位和既有 macOS 精度口径。

## 3. 运行态结果

| 门禁 | macOS | Windows |
| --- | --- | --- |
| 启动探针 `ready` | 通过 | 通过 |
| `byte_count shared_primary status=succeeded` | 通过 | 通过 |
| `rate shared_primary status=succeeded` | 通过 | 通过 |
| `compact_rate shared_primary status=succeeded` | 通过 | 通过 |
| 单进程同类正向事件去重 | 通过 | 通过 |
| `native_fallback` | 无 | 无 |
| 旧生产 `status=matched` | 无 | 无 |
| 影子或失败事件 | 无 | 无 |
| 现行展示保持 | 通过 | 通过 |
| 启动、连接与性能 | 通过 | 通过 |

诊断未出现敏感内容、重复失控或 `WINDOWS_FAILURE`。双端既有窗口层级、单实例和系统交互行为未发现回归。

## 4. 工程门禁

- P1.4-0 至 P1.4-4 分别由 PR #42 至 PR #46 合并，依次建立方案、懒回退路由并切换三类格式。
- 最终验收基线的 [Windows 构建与测试 run 31560536083](https://github.com/HongXunPan/mihomo-meter/actions/runs/31560536083) 已通过，验收产物来自该 run。
- 同一主分支基线的[通用 CI run 31560536099](https://github.com/HongXunPan/mihomo-meter/actions/runs/31560536099) 已通过。
- macOS 与 Windows 双端实机结果由用户最终确认。

## 5. 放行边界

P1.4 双端运行态门禁通过，三类流量格式正式保持共享优先；共享调用、渲染或结果异常时，仍按单次调用立即回退原生文本。

本次通过不删除原生格式化实现，不移除 P1.3 对照路由和确定性差分测试，也不扩大到 View、窗口生命周期、网络、Controller 解码、凭据、SQLite、本地化文案或异步调度。Proxy 类型归一化与分类、配额目标点数和采样仍是后续候选，必须另行评估、规划和验收。
